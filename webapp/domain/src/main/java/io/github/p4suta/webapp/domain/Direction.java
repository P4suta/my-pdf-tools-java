package io.github.p4suta.webapp.domain;

/** The reading direction of the composed two-page spreads. */
public enum Direction {
    /** Right-to-left (the default for vertical-text Japanese books). */
    RTL,
    /** Left-to-right. */
    LTR
}
